<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.laureano.ta_te_ti">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_tic_tac_toe"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name="com.laureano.ta_te_ti.TablaJugadoresActivity"
            android:windowSoftInputMode="adjustPan"
            >
        </activity>
        <!-- android:theme="@style/AppTheme" -->
        <activity android:name="com.laureano.ta_te_ti.Tateti"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name="com.laureano.ta_te_ti.Tateti_Single" />

        <activity android:name="com.laureano.ta_te_ti.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>